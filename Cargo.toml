[profile.release]
panic = "abort"
lto = "fat"
strip = true
debug = false
opt-level = "z"
codegen-units = 1

[package]
name = "hostname_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = {version = "4.5.49", features = ["derive"]}
